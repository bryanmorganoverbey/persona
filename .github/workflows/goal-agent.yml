# Updated workflow for goal-agent

name: Goal Agent

actions:
  - name: Checkout code
    uses: actions/checkout@v2

  - name: Pull latest changes
    run: git pull origin main

  - name: Run your commands
    run: |
      # Your existing commands here
      echo 'Running the goal agent workflow'